/// Represents the application theme mode independently of any UI framework.
enum AppThemeMode {
  /// Use the system default brightness.
  system,

  /// Always use the light theme.
  light,

  /// Always use the dark theme.
  dark,
}
